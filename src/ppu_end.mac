		.EVEN
PP_END:				; end of the block copied into PPU RAM

; This block is the last thing in the image (see src/exolon.list) and
; is dead CPU RAM once PP_MAIN_LOAD has pushed it into PPU RAM, so the
; back buffer starts below it and overlays it - which is where the
; game found the room for a second sound engine.
